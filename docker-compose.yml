version: '3.8'

services:
  cman:
    image: rjchicago/cman:${VERSION:-latest}
    build: ./app
    stdin_open: true
    tty: true
    environment:
      - TERM=xterm-256color
    volumes:
      - ./app/src:/app/src
      - ./app/levels:/app/levels